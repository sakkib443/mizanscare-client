"use client";

/**
 * Exam Security Component — CURRENTLY DISABLED
 * Re-enable when needed by restoring the fullscreen lock code.
 */
export default function ExamSecurity({ examId, onViolationLimit = () => { } }) {
    return null; // Security OFF — re-enable later
}
